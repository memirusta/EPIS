# Training

The private EPIS project treats the accumulated conversational dataset as a portable identity record and uses adapter/fine-tuning experiments to improve style consistency.

**No real conversations are published here.** `final_dataset.example.jsonl` contains synthetic examples only.

For real deployments, keep training data private, review it before training, and avoid including secrets or third-party private messages.
